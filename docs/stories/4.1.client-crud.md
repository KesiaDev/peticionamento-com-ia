# Story 4.1: Client Database & CRUD

## Status
InProgress

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "eslint"]

## Story
**As a** secretária,
**I want** cadastrar e gerenciar clientes do escritório,
**so that** eu possa manter um registro organizado de todas as pessoas atendidas.

## Acceptance Criteria
1. Migration `00006_create_clients.sql` cria tabela `clients` com RLS (isolamento por `organization_id`)
2. Página `/clients` com tabela exibindo: nome, CPF/CNPJ, email, telefone, data de cadastro
3. Botão "Novo Cliente" abre formulário com campos: nome completo, tipo documento (CPF/CNPJ), número, email, telefone, endereço (jsonb), notas
4. Validação de CPF/CNPJ no frontend (Zod)
5. Edição inline ou em modal
6. Soft delete (campo `deleted_at`)
7. Busca por nome, CPF/CNPJ ou email
8. Paginação e ordenação

## Tasks / Subtasks

### Task 1: Migration — Tabela `clients` (AC1)
- [x] 1.1 Criar migration `supabase/migrations/00007_create_clients.sql`
- [x] 1.2 Definir tabela `clients` com colunas: `id` (uuid PK), `organization_id` (uuid FK → organizations), `full_name` (text NOT NULL), `document_type` (text CHECK IN ('cpf','cnpj')), `document_number` (text), `email` (text), `phone` (text), `address` (jsonb), `notes` (text), `created_at` (timestamptz), `updated_at` (timestamptz), `deleted_at` (timestamptz NULL), `created_by` (uuid FK → profiles)
- [x] 1.3 Criar índice único em `(organization_id, document_number)` WHERE `deleted_at IS NULL`
- [x] 1.4 Criar índice GIN em `full_name` para busca (pg_trgm)
- [x] 1.5 Habilitar RLS: política SELECT/INSERT/UPDATE/DELETE restrita a `organization_id` via profiles subquery
- [x] 1.6 Filtrar `deleted_at IS NULL` nas políticas de SELECT

### Task 2: Tipos TypeScript e Schema Zod (AC3, AC4)
- [x] 2.1 Criar tipo `Client` em `src/types/client.ts`
- [x] 2.2 Criar schema Zod `clientFormSchema` em `src/schemas/client.schema.ts` com validação de CPF/CNPJ
- [x] 2.3 Implementar funções de validação `isValidCPF(value: string): boolean` e `isValidCNPJ(value: string): boolean`
- [x] 2.4 Adicionar custom Zod refinement que seleciona validação com base em `document_type`

### Task 3: Supabase Service Layer (AC1, AC6)
- [x] 3.1 Criar `src/services/clients.ts` com funções CRUD
- [x] 3.2 `fetchClients(params: { search?, page, pageSize, sortBy, sortOrder })` — com filtro `deleted_at IS NULL`
- [x] 3.3 `getClientById(id: string)`
- [x] 3.4 `createClient(data: ClientFormData)`
- [x] 3.5 `updateClient(id: string, data: ClientFormData)`
- [x] 3.6 `softDeleteClient(id: string)` — set `deleted_at = now()`
- [x] 3.7 Implementar busca ILIKE em `full_name`, `document_number`, `email` (AC7)

### Task 4: Página de Listagem `/clients` (AC2, AC7, AC8)
- [x] 4.1 Criar rota `/clients` em `src/pages/clients/ClientsPage.tsx`
- [x] 4.2 Implementar shadcn/ui Table com colunas: Nome, CPF/CNPJ, Email, Telefone, Data Cadastro
- [x] 4.3 Adicionar campo de busca com debounce (300ms) filtrando por nome, CPF/CNPJ ou email (AC7)
- [x] 4.4 Implementar paginação server-side com controles prev/next e seleção de tamanho de página (AC8)
- [x] 4.5 Implementar ordenação por clique no header das colunas (AC8)
- [x] 4.6 Adicionar botão "Novo Cliente" no header da página (AC3)
- [x] 4.7 Adicionar ações por linha: Editar, Excluir (com confirmação)

### Task 5: Formulário de Criação/Edição (AC3, AC4, AC5)
- [x] 5.1 Criar componente `src/components/clients/ClientForm.tsx` (modal Dialog)
- [x] 5.2 Integrar React Hook Form com Zod resolver (`clientFormSchema`)
- [x] 5.3 Campos: nome completo (text), tipo documento (select CPF/CNPJ), número documento (masked input), email, telefone (masked), endereço (campos estruturados em jsonb: rua, número, complemento, bairro, cidade, UF, CEP), notas (textarea)
- [x] 5.4 Aplicar máscara de CPF (###.###.###-##) e CNPJ (##.###.###/####-##) dinamicamente ao trocar tipo
- [x] 5.5 Exibir erros de validação inline por campo
- [x] 5.6 Modo edição: preencher formulário com dados existentes (AC5)
- [x] 5.7 Feedback de sucesso/erro com toast (shadcn/ui Sonner)

### Task 6: Soft Delete com Confirmação (AC6)
- [x] 6.1 Criar componente `src/components/clients/ClientDeleteDialog.tsx` (AlertDialog)
- [x] 6.2 Ao confirmar, chamar `softDeleteClient(id)` e atualizar listagem
- [x] 6.3 Exibir toast de confirmação de exclusão

### Task 7: Testes (todos ACs)
- [ ] 7.1 Testes unitários para `isValidCPF` e `isValidCNPJ` — `src/schemas/__tests__/client.schema.test.ts`
- [ ] 7.2 Testes unitários para `clientFormSchema` (casos válidos e inválidos)
- [ ] 7.3 Testes do service layer com mocks do Supabase — `src/services/__tests__/client.service.test.ts`
- [ ] 7.4 Testes de componente para `ClientFormDialog` — `src/components/clients/__tests__/ClientFormDialog.test.ts`
- [x] 7.5 Verificar lint (`npm run lint`) e typecheck (`npm run typecheck`)

## Dev Notes

### Estrutura de Arquivos
```
src/
├── pages/
│   └── ClientsPage.tsx
├── components/clients/
│   ├── ClientFormDialog.tsx
│   ├── ClientDataTable.tsx
│   ├── DeleteClientDialog.tsx
│   └── columns.tsx              # definição de colunas DataTable
├── schemas/
│   └── client.schema.ts
├── services/
│   └── client.service.ts
├── types/
│   └── client.ts
supabase/migrations/
└── 00006_create_clients.sql
```

### Validação CPF/CNPJ

**CPF — Algoritmo de validação:**
```typescript
function isValidCPF(cpf: string): boolean {
  const cleaned = cpf.replace(/\D/g, '');
  if (cleaned.length !== 11) return false;
  if (/^(\d)\1{10}$/.test(cleaned)) return false; // rejeita sequências iguais

  let sum = 0;
  for (let i = 0; i < 9; i++) sum += parseInt(cleaned[i]) * (10 - i);
  let remainder = (sum * 10) % 11;
  if (remainder === 10) remainder = 0;
  if (remainder !== parseInt(cleaned[9])) return false;

  sum = 0;
  for (let i = 0; i < 10; i++) sum += parseInt(cleaned[i]) * (11 - i);
  remainder = (sum * 10) % 11;
  if (remainder === 10) remainder = 0;
  return remainder === parseInt(cleaned[10]);
}
```

**CNPJ — Algoritmo de validação:**
```typescript
function isValidCNPJ(cnpj: string): boolean {
  const cleaned = cnpj.replace(/\D/g, '');
  if (cleaned.length !== 14) return false;
  if (/^(\d)\1{13}$/.test(cleaned)) return false;

  const weights1 = [5,4,3,2,9,8,7,6,5,4,3,2];
  const weights2 = [6,5,4,3,2,9,8,7,6,5,4,3,2];

  let sum = 0;
  for (let i = 0; i < 12; i++) sum += parseInt(cleaned[i]) * weights1[i];
  let remainder = sum % 11;
  const digit1 = remainder < 2 ? 0 : 11 - remainder;
  if (parseInt(cleaned[12]) !== digit1) return false;

  sum = 0;
  for (let i = 0; i < 13; i++) sum += parseInt(cleaned[i]) * weights2[i];
  remainder = sum % 11;
  const digit2 = remainder < 2 ? 0 : 11 - remainder;
  return parseInt(cleaned[13]) === digit2;
}
```

**Máscaras de Input:**
- CPF: `###.###.###-##`
- CNPJ: `##.###.###/####-##`
- Telefone: `(##) #####-####` ou `(##) ####-####`
- CEP: `#####-###`

### Supabase Storage (referência para Story 4.2)
- Bucket: `client-documents`
- Path pattern: `{organization_id}/{client_id}/{filename}`
- Políticas de acesso via RLS no Storage

### shadcn/ui DataTable
- Usar `@tanstack/react-table` via shadcn DataTable recipe
- Server-side pagination com `from/to` do Supabase `.range()`
- Sorting via `.order()` no query builder

### Endereço (jsonb)
```typescript
interface ClientAddress {
  street: string;
  number: string;
  complement?: string;
  neighborhood: string;
  city: string;
  state: string; // UF (2 chars)
  zip_code: string; // CEP
}
```

### React Hook Form Pattern
```typescript
const form = useForm<ClientFormData>({
  resolver: zodResolver(clientFormSchema),
  defaultValues: { document_type: 'cpf', ... }
});
```

### Testing
- **Vitest** para testes unitários e de componente
- Arquivos de teste em `__tests__/` adjacentes aos módulos
- Mock do Supabase client com `vi.mock('@/lib/supabase')`
- Testing Library para componentes React
- Testar validação CPF/CNPJ com valores válidos, inválidos e edge cases (sequências repetidas, tamanho incorreto)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-17 | 1.0 | Story created | River (SM) |

## Dev Agent Record
### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
- TypeScript typecheck: passed (0 errors)
- ESLint: passed (0 new errors; 3 pre-existing errors in UI components)

### Completion Notes List
- Migration file numbered 00007 (not 00006 as originally stated in AC1) because 00004 already exists and the instruction parameter specified 00007
- Used `profiles` subquery for RLS instead of `auth.jwt()->>'organization_id'` to match existing RLS pattern from migration 00003
- Service file named `clients.ts` (matching `members.ts` pattern) instead of `client.service.ts`
- Form component named `ClientForm.tsx` instead of `ClientFormDialog.tsx` (simpler naming)
- Added `created_by` FK to profiles as specified in requirements
- pg_trgm extension enabled for GIN index on full_name
- Task 7 (unit tests) left unchecked — to be completed in QA phase

### File List
- `supabase/migrations/00007_create_clients.sql`
- `src/types/client.ts`
- `src/schemas/client.schema.ts`
- `src/services/clients.ts`
- `src/hooks/useClients.ts`
- `src/pages/clients/ClientsPage.tsx`
- `src/components/clients/ClientForm.tsx`
- `src/components/clients/ClientDeleteDialog.tsx`
- `src/App.tsx` (modified — added ClientsPage import and /clients route)

## QA Results

**Reviewer:** Quinn (@qa) | **Date:** 2026-03-18 | **Verdict: PASS WITH CONCERNS**

### AC Coverage

| AC | Status | Notes |
|----|--------|-------|
| AC1 — Migration with RLS | PASS | Table, indexes, RLS policies, pg_trgm extension, updated_at trigger all correct |
| AC2 — /clients page with table | PASS | Table displays nome, CPF/CNPJ (with badge+mask), email, telefone, data cadastro |
| AC3 — "Novo Cliente" form | PASS | Modal dialog with all required fields including structured address |
| AC4 — CPF/CNPJ validation (Zod) | PASS | Correct algorithms, custom refinement, dynamic mask switching |
| AC5 — Edit in modal | PASS | Form pre-fills with existing data, masks applied correctly |
| AC6 — Soft delete | PASS | `deleted_at = now()`, RLS SELECT filters `deleted_at IS NULL`, service filters too |
| AC7 — Search by name/CPF/email | PASS | ILIKE with debounce 300ms, resets page on search change |
| AC8 — Pagination and sorting | PASS | Server-side range(), sortable columns, page size selector |

### Code Quality Issues

1. **BUG (Medium):** In `ClientsPage.tsx`, clicking Edit or Delete buttons inside a table row also triggers the row's `onClick={() => navigate(...)}` due to event propagation. The action buttons do not call `e.stopPropagation()`. This means clicking Edit navigates to `/clients/:id` AND opens the form, and clicking Delete navigates AND opens the delete dialog.
2. **BUG (Low):** Migration is numbered `00007` but AC1 says `00006`. Dev documented this discrepancy in completion notes (acceptable, but AC text should be updated to match).
3. **MINOR:** `document_number` is optional in the schema (`z.string().optional().default("")`) but the Zod refinement calls `.replace(/\D/g, "")` on it. This works because of the default, but could be clearer.
4. **GOOD:** Migration quality is excellent — unique partial index on `(org_id, document_number) WHERE deleted_at IS NULL`, GIN trigram index for search, proper RLS with profiles subquery pattern.
5. **GOOD:** Service layer strips formatting before DB writes (`replace(/\D/g, "")`), stores clean numbers.
6. **GOOD:** Form resets correctly on open/close, handles both create and edit modes.

### Security
- RLS isolates by organization_id via profiles subquery: PASS
- SELECT policy filters deleted_at IS NULL: PASS
- Service layer also filters deleted_at: PASS (defense in depth)
- created_by tracked: PASS

### CPF/CNPJ Validation Accuracy
- **CPF algorithm:** Correct. Rejects all-same-digit sequences, validates both check digits with proper modular arithmetic.
- **CNPJ algorithm:** Correct. Proper weight arrays `[5,4,3,2,9,8,7,6,5,4,3,2]` and `[6,5,4,3,2,9,8,7,6,5,4,3,2]`, remainder logic (`< 2 ? 0 : 11 - remainder`) is accurate.
- **Masks:** CPF `###.###.###-##`, CNPJ `##.###.###/####-##`, Phone `(##) #####-####` / `(##) ####-####` — all correct.
- **Edge cases:** Empty document allowed (optional field). All-same-digits rejected. Formatting stripped before validation.

### Tests
- Tasks 7.1-7.4 deferred. No tests exist yet. **Must be addressed before Done.**

### UI Quality
- Responsive grid layout on form (sm:grid-cols-2, sm:grid-cols-3): PASS
- Loading skeletons: PASS
- Empty states (no clients, no search results): PASS
- Toast feedback on all mutations: PASS
- Scrollable dialog for long form: PASS (max-h-[90vh] overflow-y-auto)
